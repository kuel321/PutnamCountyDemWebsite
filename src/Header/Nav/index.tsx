'use client'

import React from 'react'

import type { Header as HeaderType } from '@/payload-types'

// Nav is now rendered directly in Component.client.tsx
export const HeaderNav: React.FC<{ data: HeaderType }> = () => {
  return <></>
}
